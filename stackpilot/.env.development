# Local development: talk to the local backend (backend/.env PORT=5000).
# Loaded only by `npm run dev` (Vite development mode); production builds ignore it
# and fall back to the Railway URL in utils/api.ts.
VITE_API_BASE_URL=http://localhost:5000/api
